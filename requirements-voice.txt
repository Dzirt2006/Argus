faster-whisper>=1.1
piper-tts>=1.2
kokoro-onnx>=0.4
# openwakeword pulls tflite-runtime which has no Python 3.12+ wheels.
# Install openwakeword with: pip install --no-deps openwakeword>=0.6
# The remaining deps (onnxruntime, scipy, etc.) are listed below.
onnxruntime>=1.16
scipy>=1.12
scikit-learn>=1.4
tqdm>=4
requests>=2
sounddevice>=0.5
numpy>=1.26
silero-vad>=5.1
